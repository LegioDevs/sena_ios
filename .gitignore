node_modules/
.DS_Store

# legacy-peer-deps is REQUIRED (.npmrc committed): the dev toolchain peers
# (eslint-plugin-react vs eslint 10) don't resolve under strict mode.

# dist/ is committed on purpose: it is the compiled web build shipped to
# the native project by `cap sync`. The mirror pushes a fresh one each run.

# Stale web copy inside the native project (regenerated by cap sync)
ios/App/App/public/

# xcodebuild -derivedDataPath output
ios/App/build/
